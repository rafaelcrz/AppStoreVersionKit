# This workflow will build a Swift project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-swift

name: Build and Test

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  iOS:
    name: Build and test on iOS
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Xcode
        uses: ruby/setup-xcode@v1
        with:
          xcode-version: '26.0'
      - name: Build
        run: xcodebuild build-for-testing -scheme AppStoreVersionKit -destination 'platform=iOS Simulator,name=iPhone 16'
      - name: Test
        run: xcodebuild test-without-building -scheme AppStoreVersionKit -destination 'platform=iOS Simulator,name=iPhone 16'
